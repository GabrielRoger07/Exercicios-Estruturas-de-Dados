#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    int num1, num2, resultado;
    scanf("%d", &num1);
    scanf("%d", &num2);
    resultado = num1 + num2;
    printf("%d\n", resultado);
    return 0;
}
